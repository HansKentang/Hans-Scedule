{
  "name": "haven-schedule",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild firebase-entry.js --bundle --format=iife --global-name=FirebaseSDK --outfile=firebase-bundle.js",
    "dev": "python -m http.server 8000"
  },
  "dependencies": {
    "firebase": "^11.10.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}